# 指定 redis port
port 6379
# 启用 cluster
cluster-enabled yes
# 指定 cluster config 档案
cluster-config-file nodes.conf
# 指定 node 无法连线的时间，单位毫秒
cluster-node-timeout 5000
# master 密碼
masterauth redis@cluster@2020
# 设定连线密码
requirepass redis@cluster@2020
notify-keyspace-events "Ex"